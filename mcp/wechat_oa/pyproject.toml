[project]
name = "wechat-oa-mcp-server"
version = "0.1.0"
description = "WeChat Official Account API MCP bridge server"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.uv]
package = false
