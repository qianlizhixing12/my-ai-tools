[project]
name = "weread-mcp-server"
version = "0.1.0"
description = "WeRead API MCP bridge server"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "mcp>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
